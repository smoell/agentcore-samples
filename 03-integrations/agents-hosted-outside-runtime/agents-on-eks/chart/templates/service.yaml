apiVersion: v1
kind: Service
metadata:
  name: {{ include "strands-agents-travel.fullname" . }}
  labels:
    {{- include "strands-agents-travel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "strands-agents-travel.selectorLabels" . | nindent 4 }}
